"""Notification service (placeholder).

Built out in Phase 7. All notification writes must be wrapped in try/except and
are non-fatal: a notification failure must never roll back its triggering event
(RULE 7). Phase 0 ships no notification logic.
"""

from __future__ import annotations
